---
import * as DevIcons from './devicons'

export type DevIconName = keyof typeof DevIcons

type Props = {
  class?: string
  name: DevIconName
}

const { name, class: className } = Astro.props
const DevIconComponent = DevIcons[name]
---

<DevIconComponent className={className} />
