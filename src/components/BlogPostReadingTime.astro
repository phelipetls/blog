---
import { Timer } from 'lucide-react'

type Props = {
  readingTime: number
}

const { readingTime } = Astro.props
---

<div class='flex items-center gap-2'>
  <Timer size='1.25em' className='shrink-0' />

  <div>{Math.floor(readingTime)} min.</div>
</div>
