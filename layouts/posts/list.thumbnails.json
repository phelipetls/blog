[
  {{- range $i, $page := .Pages -}}
    {{- if $i -}},{{- end -}}
    {
      "url": "{{ ($page.OutputFormats.Get "thumbnail").Permalink | safeURL }}",
      "dir": "{{ .File.Dir }}"
    }
  {{- end -}}
]
