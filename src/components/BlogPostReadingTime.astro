---
import { BookOpen } from 'lucide-react'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
type Props = {
  readingTime: number
}

const { readingTime } = Astro.props
---

<div class="flex items-center gap-2">
  <BookOpen />

  <div>{Math.floor(readingTime)} min.</div>
</div>
